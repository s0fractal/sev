# Round 6 — Kimi, 2026-08-10, deep review, 6.7/10

Target: commit `9fe95d7c3a26988c453e17c47ccf2d3d405ccfa6` (research
bootstrap) — **the first round with an exact SHA**. Reviewer independently
ran the model (56/56 PASS, exit 0 — verified, not taken on faith) and read
all four artifacts plus the ecosystem context (warrant/proposals, oaip,
protocol-ecosystem).

## Findings

| # | Sev | Finding |
|---|---|---|
| K1 | P1 | The repo violates its own thesis: README cites `reviews/` and `conformance/` — neither existed; five review rounds lived only as appendix summaries, i.e. *asserted* provenance in a repo whose purpose is converting asserted provenance into verified |
| K2 | P1 | The projector does not exist: all 56 vectors cover SEV's *inputs* (snapshot, receipt, bijection); zero code for skolemization, named graphs, canonical N-Quads, view-manifest, loss_manifest; profile N1–N7 are pure prose — SEV is "the ecosystem's strictest judge that has never issued a verdict" |
| K3 | P1 | Process decorrelates from rules: squash bootstrap directly on master (against AGENTS.md rule 1); rounds 1–5 have no SHAs so "review against an exact SHA" was unfalsifiable; no CI — exit-status honesty a habit, not a gate |
| K4 | P2 | Blocking chain has no critical path: receipt not filed in warrant/proposals; OAIP/BOS receipts nonexistent; nobody upstream knows they are blocking SEV |
| K5 | P2 | Review spiral without a ceiling: five AMEND rounds in ~2 days, each adds normative surface; no criterion for "enough"; every round raises the cost of the second implementation |
| K6 | P3 | parse_receipt/parse_snapshot asymmetry; hardcoded `"0.4"` in the composed verdict; vectors exist only as Python (a second implementation would translate, not independently implement); fixture body `"warrant": "0.2"` vs contract `"0.4"` uncommented; namespace strategy inconsistent (s0fractal.dev vs github URL) |

## Recommendations → dispositions (this branch)

| Rec | Disposition |
|---|---|
| R1 publish `reviews/` | **Done** — rounds 1–6 filed; rounds 1–5 explicitly marked as reconstructed/unversioned (the condition cannot be repaired, only stopped); from round 6 a review without a SHA is invalid |
| R2 CI gate | **Done** — `.github/workflows/model.yml` runs the model and the projector on every push/PR |
| R3 language-neutral vectors | **Started** — `conformance/parse-strict.vectors.json` (byte-level fixtures, base64 + expected codes) generated from the model; remaining vector families to follow before any second implementation |
| R4 projector MVP | **Done** — `model/sev_projector.py`: Warrant-quadrant-only projection of a verified (snapshot, receipt) pair into canonical N-Quads + view-manifest + loss_manifest; end-to-end vectors incl. cross-process byte-identity; the first real SEV output exists |
| R5 file receipt upstream (warrant/proposals or issue) | **Held** — outward action into another repo's governance; needs explicit human authorization (AGENTS.md rule 1 there and here) |
| R6 freeze minimal v0 core | **Drafted** — freeze-candidate sections added to spec and proposal; freeze itself is a maintainer act after the next clean gate (exit criterion adopted: a round with zero P1 → freeze) |
| R7 map decay CI + visibility column | **Held** — belongs to protocol-ecosystem; queued as its own change |
| R8 code smalls | **Done** — `expected_version` parameter; `parse_receipt(raw, snapshot, cas)` symmetric composed entrypoint; fixture version comment; namespace note left as profile open question |

## Scores (reviewer's)

Concept 9 · contract rigor 8.5 · executability 5 · status honesty 9.5 ·
process discipline 5 · ecosystem integration 4 · distance to v0 6 —
**overall 6.7/10**: "the strongest process and contracts in the ecosystem,
the weakest presence of its own product."
