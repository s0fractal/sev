# Re-gate of `6019ff27…` — Codex, 2026-08-10, verdict AMEND (hold merge)

Target: PR #1 head `6019ff27ef6949ff4f84cc22a52c44425e49dbcd`. Baseline
64 + 120 + 9 green. The aggregate signature invariant was accepted, and the
vendored upstream fixture independently checked: digest `bcd9765d…` matches,
the file is byte-identical to the current `warrant/examples/accept.warrant.json`,
and the provenance pin is non-vacuous. One P1 — opened by that very fixture.

## P1 — `not-applicable` was projected as an executed CheckRun

The upstream record honestly reports `runtime: cmd@v1`,
`re_execution: not-applicable` — and the graph nevertheless emitted
`rdf:type sigma:CheckRun`, `prov:used`, `prov:wasInformedBy`, while the
manifest listed `check-run` in coverage and `L-REEXEC` among the losses. One
dataset said all at once: execution does not apply here; a CheckRun existed;
it used the reason; the graph records a past re-execution.

Even without an explicit `rdf:type prov:Activity`, `prov:used` and
`prov:wasInformedBy` carry an Activity domain in PROV-O, so the node becomes
an Activity under entailment — the observational collapse warrant SPEC §7
exists to prevent.

**Disposition — assessment and execution split.** Every reason now gets an
`sev:ExecutionAssessment` (an Entity: `sev:assesses` the reason,
`sigma:reExecution`, `sigma:failureCode`, `sev:receiptCoreDigest`). A
`sigma:CheckRun` — and every Activity-domain PROV relation — is emitted only
for `matched`/`mismatched`, linked from the assessment by `sev:executedAs`.
`check-run` enters coverage and `L-REEXEC` the loss manifest only when a run
was actually emitted.

Vectors: the upstream `not-applicable` record yields an assessment and no
run, no coverage claim, no `L-REEXEC`; `unverified` likewise, with no PROV
execution relation anywhere; `matched` and `mismatched` do produce runs with
`prov:used`; switching `not-applicable → matched` changes topology, coverage
and losses, not merely a literal.

**Independent entailment guard.** A vector parses the emitted N-Quads back
and asserts that every subject of an Activity-domain predicate
(`prov:used`, `prov:wasInformedBy`, `prov:generated`,
`prov:wasAssociatedWith`) is typed `sigma:CheckRun` — checked against the
output, not trusted from the emitter, and run over three datasets.

## Mutation results

| Guard | Suite when removed |
|---|---|
| Activity gating (`matched`/`mismatched` only) | fails |
| the assessment node itself | fails |

## State after closure

64 model + 130 projector vectors + 9 fixtures, all green, exit-status
honest. Profile §4.4 carries the assessment/run split. Freeze criterion
still unmet.
