# Re-gate of `9f095052…` — Codex, 2026-08-10, verdict AMEND (hold merge)

Target: PR #1 head `9f0950528ac9733b0badfd31e91ecc985f0862de`. Baseline
64 + 46 + 9 green; the registry, reason/run split and subroot scoping hold.
Four P1s and one P2, all on the boundary of **the relations the graph
asserts**. No GitHub write, push or review-write by the reviewer.

## Findings → dispositions (same PR, narrow closure)

| # | Sev | Finding | Disposition |
|---|---|---|---|
| 1 | P1 | The "exact bijection" was a dict keyed by path — last-wins. Two sources sharing a path (one forged digest, one real, forged sorting first) passed cleanly, and the projector emitted `urn:wrt:blob:000…` for a member absent from the snapshot | Bijection over **pairs**: `(path, digest)` multisets, plus `DUPLICATE_SOURCE_PATH` for a repeated path regardless of digest. Vector: forged + real on one path → refusal, no forged blob |
| 2 | P1 | Ordering keyed on `(runtime, semantics_digest)` made two `ski@v1` entries with different anchors "correctly sorted", after which the lookup and the projector took last-wins — the receipt held two answers to "under which semantics did this run" and the graph silently picked one | Uniqueness by **runtime** (`DUPLICATE_RUNTIME`); ordering by runtime alone. One reason resolves to exactly one policy entry |
| 3 | P1 | The graph asserted `prov:used` on a check blob that exists in neither CAS nor universe — including on an honest `unverified/MISSING_BLOB`, where nothing was used at all — and it hung that edge on the **Reason**, whose PROV domain is an Entity, re-fusing the reason/run split made one round earlier | `CHECK_BLOB_ABSENT`: `matched`/`mismatched` require the committed check to be present as a source in the same subroot (warrant SPEC §6 resolves the check as a blob; §6(7) keeps re-ran and could-not-run distinct). The Reason carries only weak `wrt:checkRef` (literal) and, when sealed, `wrt:checkBlob`; `prov:used` is emitted **only** on a `CheckRun` that actually executed. Both fixtures now seal a real check blob |
| 4 | P1 | `_committed_reasons()` re-read the CAS after the verdict was already rendered — the same "verdict over one snapshot, assertion over another" class the sealed pipeline exists to prevent; a store mutating in between silently dropped edges | The validators emit an immutable **validated view** (`view=` out-param carrying the parsed committed reasons per source), and the projector consumes only that: it touches no store after validation. Vector: projecting over a one-shot store that raises on any post-validation read yields byte-identical output |
| 5 | P2 | The profile's normative identity table and §4.4 still described the old single `urn:sigma:run(WID, ptr, digest)` object; the correct split lived only in amendment notes | §2 and §4.4 rewritten as the source of truth (Reason vs CheckRun, `wrt:checkRef`/`wrt:checkBlob`, subroot-scoped sources); the ledger keeps history only |

All three new validator guards were verified **non-vacuous** by mutation:
removing each one individually makes the suite fail.

## State after closure

64 model + 52 projector vectors + 9 fixtures, all green, exit-status honest.
Freeze criterion still unmet; merge stays held.
