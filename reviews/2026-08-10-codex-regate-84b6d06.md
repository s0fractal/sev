# Re-gate of `84b6d06c…` — Codex, 2026-08-10, verdict AMEND (hold merge)

Target: PR #1 head `84b6d06c9b6bbaf33f8328f17b91b2509d590ee0`; local/origin/PR
agree, CI green, baseline 64 + 103 + 9 confirmed. One new P1. No files,
commits, pushes or GitHub write actions by the reviewer.

## P1 — any issue at the same pointer legalised malformed evidence

The malformed-occurrence join reduced each issue to its `at.value`: neither
`code` nor `severity` nor the kind of occurrence was checked. Reproduced
with honest counts (`warnings: 1`, `errors: 0`, `ok: true`):

```json
{"sigs": [7],
 "issues": [{"code": "UNRELATED_WARNING", "severity": "WARN",
             "at": {"kind": "json-pointer", "value": "/sigs/0"}}]}
```

→ zero findings, output produced, `sources_excluded: 0`, no `signature` in
`coverage.not_emitted`, no `L-NOSIG`. So an arbitrary WARN at the right
address satisfied "malformed reported", kept the record in the graph, and
let coverage call a malformed signature *no signature evidence* — directly
refuting the previous round's claim that reporting "carries the record into
exclusions honestly".

**Disposition — a normative matrix, not one universal severity.** The join
now requires an issue matching `(pointer, code, severity)`, and the severity
follows warrant SPEC §5 rather than a blanket rule:

| Occurrence | Code | Severity |
|---|---|---|
| `sigs` not a list | `MALFORMED_ENVELOPE` | ERR |
| malformed signature, a reported-valid signature by `body.actor.id` surviving | `MALFORMED_SIGNATURE` | WARN or ERR |
| malformed signature, no surviving valid actor-signature | `MALFORMED_SIGNATURE` | ERR |
| `because` not a list | `MALFORMED_BODY_SCHEMA` | ERR |
| malformed `because` entry | `MALFORMED_REASON` | ERR |

Evidence **presence** for `coverage`/`L-NO*` is now derived from the total
derivation and published through the validated view, so a malformed
occurrence counts as evidence the input held even though it is represented
by an issue rather than an entry.

Six permanent vectors, exactly the reviewer's list: unrelated WARN →
refusal; right code + wrong severity → refusal; signature code over a
malformed reason → refusal; the correct tuple → accepted, record excluded,
`L-NOSIG` still emitted; malformed extra co-signature beside a valid actor
signature → WARN path accepted with the record still projected.

## Mutation results

| Guard | Suite when removed |
|---|---|
| `(pointer, code, severity)` join | fails |
| severity matrix (WARN allowed only with a surviving actor signature) | fails |
| coverage presence from total derivation | fails |

## State after closure

64 model + 110 projector vectors + 9 fixtures, all green, exit-status
honest. Proposal rev 9 and the profile carry the matrix and the
presence-derivation rule. Freeze criterion still unmet.
