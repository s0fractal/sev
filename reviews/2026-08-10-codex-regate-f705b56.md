# Re-gate of `f705b567…` — Codex, 2026-08-10 — artifact-scoped

Target: PR #1 head `f705b5672d6ae33488ecda55e6b0ec6cd418fb50`.

| Artifact | Verdict |
|---|---|
| snapshot + receipt core | **AMEND — P1** (+ one P2) |
| MVP projector | **AMEND — inherits the same P1** |
| full target profile | out of scope; status unchanged |

The previous round is confirmed closed: `CAS_REQUIRED`, the separately named
structural API, sink clearing, and the ordinary hostile `repr`/bytes-subclass
paths all hold.

## P1 (core + MVP) — the receipt still chose whether its bytes were read

`loaded: false` skipped the entire byte-derived layer for that source. Any
ERR — even a normatively plausible `RECORD_UNREADABLE` — then made a
fabricated claim validate. On the positive `ski_fixture()`, with the record's
bytes present in the store, digest matching and a clean strict parse:

```
validator findings: []
project findings:   []
output emitted:     True
record IRI emitted: False
```

So a producer could declare a record unreadable and the MVP would remove it
from the graph, while the same composed verdict held the bytes that refute
the claim.

**Disposition — option 1 of the two offered: `loaded` is objective derived
state.** It now means exactly *the consumer obtained these bytes and the
digest matched*; parse and schema failures are issues, not un-loadedness.
Every member is resolved once, disagreement in either direction is
`LOADED_MISREPORTED`, and byte-derived checks follow the derived value.

Vectors: a fabricated unreadable record over readable bytes (refused); a
genuinely inaccessible member (accepted as unloaded, its absence still
reported); the mirror case, claiming loaded over missing bytes (refused);
the same rule on a blob member; and the projector refusing rather than
dropping the source.

## P2 (core) — `producer` was never validated

The proposal claims closed schemas while `producer` accepted an integer, an
empty object, unknown members and malformed digests. Now a closed schema
with exact keys and types — host-local means outside consensus identity, not
without a wire contract — so `validate_structure_only()` can honestly call
itself structural.

## Mutation results

| Element | Suite when removed |
|---|---|
| `LOADED_MISREPORTED` derivation | fails (model **and** projector) |
| producer schema | fails |
| gating on the derived rather than reported value | *passes* — labelled unisolatable in code: a misreport already fires the finding above, so both gates refuse the same inputs. Kept so byte derivation follows the bytes if that finding is ever softened |

## State after closure

87 model + 167 projector vectors + 9 fixtures, all green. Proposal rev 14
carries both rules. Neither the core nor the MVP had a clean round.
